version: 1

goos: linux
goarch: arm64

env:
  - CGO_ENABLED=0

flags:
  - -trimpath

ldflags:
  - "-X github.com/trganda/codeql-development-toolkit/cmd.Version={{ .Env.VERSION }}"

main: ./cmd/qlt

binary: qlt-linux-arm64
