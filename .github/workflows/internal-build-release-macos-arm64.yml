name: 🍏 Build Release (MacOS arm64)

on:
  workflow_call:
    inputs:
      version:
        description: |
          The version to release. (eg: 0.1.2, exclude the 'v'.).
        required: true
        type: string

jobs:
  build-macos-arm64:
    runs-on: macos-14 # arm64

    steps:
    - uses: actions/checkout@v4

    - name: Setup Go
      uses: actions/setup-go@v5
      with:
        go-version-file: go.mod

    - name: Build
      run: make build VERSION=${{ inputs.version }}

    - name: Build Archive
      run: |
        ARCHIVE="$(pwd)/qlt-macos-arm64.zip"
        pushd dist
        zip -r "$ARCHIVE" qlt
        popd

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: qlt-dist-macos-arm64
        path: qlt-macos-arm64.zip

    - name: Upload to GitHub Release
      run: gh release upload v${{ inputs.version }} ./qlt-macos-arm64.zip
      env:
        GH_TOKEN: ${{ github.token }}
